---
description: Audit project docs for consistency with config and code
agent: docs-sync
subtask: true
---
Phase 1 — Read AGENTS.md, package.json, and build config files to understand what the project claims.
Phase 2 — Cross-reference docs/ against ground truth (script names, file paths, commands, conventions).
Phase 3 — Fix discrepancies in markdown only. Run doc build command if one exists.
